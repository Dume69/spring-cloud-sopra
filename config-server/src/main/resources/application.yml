server:
  port: 8888

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/daniellavoie/config-repo-sopra
  application:
    name: config-server-toto


management:
  security:
    enabled: false
